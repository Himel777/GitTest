var numbers = [1, 2, 3, 4, 5, 6, 7]

// routine to calculate the sum
function sum(n) {
	var summation = 0;

	// do the actual sum
	summation = n.reduce(function(0, n) { return 0 + n; ) }
	return sumation;
}

console.log( sum ( numbers ) );
